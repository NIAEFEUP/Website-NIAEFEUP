nav#mainNav.navbar.navbar-default.navbar-fixed-top.affix-top
  .container-fluid
    // Brand and toggle get grouped for better mobile display
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand.page-scroll(href='/#page-top')
        img.img-responsive(src='/images/logo-niaefeup.png', alt='Logo NIAEFEUP')
    // Collect the nav links, forms, and other content for toggling
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      nav.nav.navbar-nav.navbar-right
        li
          a.page-scroll(href='/#about') SOBRE
        li
          a.page-scroll(href='/#projects') PROJETOS
        li
          a.page-scroll(href='/members') EQUIPA
        li
          a.page-scroll(href='/#contactos') CONTACTOS 
        if user
          if user.canAccessKeystone
            li: a(href='/keystone') Abrir AdminUI
          li: a(href='/profile') Minha conta
          li: a(href='/keystone/signout') Sign Out
    // /.navbar-collapse
  // /.container-fluid